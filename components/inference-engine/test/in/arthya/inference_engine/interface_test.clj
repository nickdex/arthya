(ns in.arthya.inference-engine.interface-test
  (:require [clojure.test :as test :refer :all]
            [in.arthya.inference-engine.interface :as inference-engine]))

(deftest dummy-test
  (is (= 1 1)))
